# MySQLTool
A mysql package above MySQL-python for more convenient database manipulation.

Besides of the methods MySQL-python offers, MySQLTool provides the following methods:

* `insert()`
* `upsert()`
* `get()`
* `update()`
* `select()`
* `delete()`
* `now()`

Most of these methods above accept passing dict as conditions to build the sql queries.